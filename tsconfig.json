{
    "include": [
        "src/**/*.ts",
        "src/**/*.vue",
        // "tests/**/*"
    ],
    "exclude": [
        "src/services/mock/json/*"
    ],
    "compilerOptions": {
        // "checkJs": true,
        "paths": {
            "@/*": ["./src/*"]
        },

        "allowJs": true,

        // Disable errors about implicit any during migration to TS:
        "noImplicitAny": false,

        // Vue/Vite recommended settings:
        "isolatedModules": true,
        "strict": true,
        "types": [
            "vite/client"
        ],

        "module": "es2020",
        "moduleResolution": "node"
    },
    "watchOptions": {
        "excludeDirectories": [
            "**/node_modules",
            "dist"
        ]
    }
}
